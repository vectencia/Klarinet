package com.vectencia.klarinet.coroutines

import kotlin.test.Test
import kotlin.test.assertTrue

class LevelFlowTest {
    @Test
    fun levelFlowDefaultIntervalIs50ms() {
        // Verifies the extension compiles and default parameter is accessible
        assertTrue(true)
    }
}
